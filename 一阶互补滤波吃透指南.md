# 一阶互补滤波吃透指南

> 目标：像 Mahony 那样，从头到尾理解每一行代码和每一个公式。

---

## 先明确坐标系约定（和 Mahony 一致）

| 坐标系 | 原点 | X 轴 | Y 轴 | Z 轴 |
|--------|------|------|------|------|
| **导航系 (n-frame, NED)** | IMU 初始位置 | 指向北 (North) | 指向东 (East) | 指向地 (Down) |
| **载体系 (b-frame, FRD)** | IMU 中心 | 指向前 (Front) | 指向右 (Right) | 指向地 (Down) |

---

## 第一部分：互补滤波的核心思想

### 1.1 为什么叫"互补"？

IMU 内部有两类传感器，它们的优缺点正好**互补**：

| 传感器 | 优点 | 缺点 | 频域特性 |
|--------|------|------|----------|
| **陀螺仪 (Gyro)** | 动态响应快、短时精度高、不受振动影响 | 积分会漂移（零偏 + 噪声累积） | 高频好，低频烂 |
| **加速度计 (Accel)** | 静态稳定（重力方向永远向下）、无漂移 | 怕线性加速度和振动（运动时测到的是"比力"而非重力） | 低频好，高频烂 |
| **磁力计 (Mag)** | 提供绝对航向参考（地磁北） | 受铁磁物质干扰（软磁/硬磁） | 慢但稳定 |

**互补滤波就是让它们各尽其才：**
- **快速变化**（高频）→ 信陀螺仪
- **长期稳态**（低频）→ 信加速度计/磁力计

### 1.2 频域视角（从信号处理角度理解）

设真实姿态角为 $\theta$，陀螺仪的测量为 $\omega$，加速度计测得的角度为 $\theta_a$：

```
陀螺路径: θ_g(s) = ω(s) / s          (积分 -> 高通特性)
加速度路径: θ_a(s)                    (直接测量 -> 低通特性)
```

让两者相加：

$$
\theta_{\text{filter}} = \underbrace{\frac{\tau s}{\tau s + 1}}_{\text{高通 (陀螺)}} \cdot \theta_g + \underbrace{\frac{1}{\tau s + 1}}_{\text{低通 (加速度)}} \cdot \theta_a
$$

两个传递函数相加 = 1，所以叫**互补**（Complementary）。

### 1.3 时域核心公式（最关键！）

把上面的式子离散化（一阶后向差分，$s \approx (1 - z^{-1}) / dt$）：

$$
\theta_n = \alpha \cdot (\theta_{n-1} + \omega_n \cdot \Delta t) + (1 - \alpha) \cdot \theta_{a,n}
$$

其中

$$
\alpha = \frac{\tau}{\tau + \Delta t}
$$

- $\tau$ 是时间常数（越大越相信陀螺仪）
- $\Delta t$ 是采样周期

**举例**：采样率 100Hz（$\Delta t$=0.01s），$\tau$=0.5s
→ $\alpha = 0.5 / (0.5 + 0.01) = 0.98$

**公式理解**：
- 第一项 $\alpha \cdot (\theta_{n-1} + \omega \Delta t)$：用陀螺仪积分预测角度
- 第二项 $(1-\alpha) \cdot \theta_a$：用加速度计测量做修正
- 两者加权相加 = 最终估计

---

## 第二部分：代码与公式逐行对应（欧拉角版本）

### 2.1 参数初始化 (`ahrs_comp_first_order_init`)

**代码**（[ahrs_comp_first_order.c:43-73](file:///workspace/Core/Src/ahrs_comp_first_order.c#L43-L73)）：

```c
ahrs->dt = 1.0f / sample_freq;

/*
 * alpha = tau / (tau + dt)
 *   tau 越大 -> alpha 越接近 1 -> 越相信陀螺仪
 *   tau = 0.5s, dt=0.01s -> alpha ≈ 0.98
 */
ahrs->alpha = tau / (tau + ahrs->dt);
```

**推导**：

从高通滤波 $H_{hp}(s) = \frac{\tau s}{\tau s + 1}$ 开始，做后向差分 $s \to (1-z^{-1})/\Delta t$：

$$
H_{hp}(z) = \frac{\tau (1-z^{-1})/\Delta t}{\tau (1-z^{-1})/\Delta t + 1}
           = \frac{\tau (1-z^{-1})}{\tau (1-z^{-1}) + \Delta t}
           = \frac{\tau - \tau z^{-1}}{(\tau + \Delta t) - \tau z^{-1}}
$$

整理成差分方程：

$$
(\tau + \Delta t) \theta_n - \tau \theta_{n-1}
= \tau (\theta_{g,n} - \theta_{g,n-1})
$$

其中 $\theta_{g,n} = \theta_{g,n-1} + \omega_n \Delta t$，再加上低通部分，最终得到：

$$
\theta_n = \underbrace{\frac{\tau}{\tau + \Delta t}}_{\alpha} \cdot (\theta_{n-1} + \omega_n \Delta t)
         + \underbrace{\frac{\Delta t}{\tau + \Delta t}}_{1-\alpha} \cdot \theta_{a,n}
$$

---

### 2.2 加速度计计算角度 (`ahrs_comp_first_order_calibrate` + `update`)

**代码**（计算加速度计的 roll/pitch）：

```c
/* 归一化 */
float a_norm = sqrtf(ax*ax + ay*ay + az*az);
float nx = ax / a_norm, ny = ay / a_norm, nz = az / a_norm;

/* FRD 坐标系下 */
accel_roll  = atan2f(ny, nz);      // 绕 X 轴
accel_pitch = asinf(-nx);           // 绕 Y 轴
```

**推导**：

在 FRD 载体系中，静止时重力向量是 $\mathbf{g}^b = [0, 0, 1]^T$（沿 Z 向下）。

当载体有姿态 (roll=$\phi$, pitch=$\theta$, yaw=$\psi$) 时，重力在载体系中的测量值为：

$$
\mathbf{a}_{\text{meas}} = R^b_n \cdot [0, 0, 1]^T
$$

其中 $R^b_n$ 是从导航系到载体系的旋转矩阵（NED→FRD）：

$$
R^b_n =
\begin{bmatrix}
\cos\theta \cos\psi & \cos\theta \sin\psi & -\sin\theta \\
\sin\phi \sin\theta \cos\psi - \cos\phi \sin\psi & \sin\phi \sin\theta \sin\psi + \cos\phi \cos\psi & \sin\phi \cos\theta \\
\cos\phi \sin\theta \cos\psi + \sin\phi \sin\psi & \cos\phi \sin\theta \sin\psi - \sin\phi \cos\psi & \cos\phi \cos\theta \\
\end{bmatrix}
$$

乘以 $[0,0,1]^T$ 得到：

$$
\mathbf{a}_{\text{meas}} =
\begin{bmatrix}
-\sin\theta \\
\sin\phi \cos\theta \\
\cos\phi \cos\theta
\end{bmatrix}
=
\begin{bmatrix}
a_x \\
a_y \\
a_z
\end{bmatrix}
$$

所以反解出：

$$
\theta = \text{pitch} = \arcsin(-a_x / \|\mathbf{a}\|)
$$
$$
\phi = \text{roll} = \arctan2(a_y, a_z)
$$

**注意**：这里用 `asinf(-nx)` 是因为 nx 已经归一化了（nx = ax/|a|）。

---

### 2.3 磁力计计算 yaw

**代码**：

```c
/* 把磁力计投影到水平面 */
float cr = cosf(gyro_roll),  sr = sinf(gyro_roll);
float cp = cosf(gyro_pitch), sp = sinf(gyro_pitch);

float mx_h = mx * cp + my * sr * sp + mz * cr * sp;
float my_h = my * cr - mz * sr;

mag_yaw = atan2f(-my_h, mx_h);
```

**原理**：
1. 先将磁力计读数用 roll/pitch 补偿（消去载体倾斜的影响）
2. 得到水平面内的磁分量 $(m_{x,h}, m_{y,h})$
3. 地磁北在 NED 系中指向 X 轴正方向（北）
4. 所以 yaw = atan2(-东, 北) = atan2(-my_h, mx_h)

---

### 2.4 融合步骤（最核心的 3 行）

**代码**（[ahrs_comp_first_order.c:168-183](file:///workspace/Core/Src/ahrs_comp_first_order.c#L168-L183)）：

```c
/* 步骤 1：陀螺仪积分 */
float gyro_roll  = ahrs->roll  + gx * dt;
float gyro_pitch = ahrs->pitch + gy * dt;
float gyro_yaw   = ahrs->yaw   + gz * dt;

/* 步骤 2：加速度计计算静态角度（见上一节） */
float accel_roll  = atan2f(ny, nz);
float accel_pitch = asinf(-nx);

/* 步骤 3：互补融合 */
float d_roll  = accel_roll  - gyro_roll;
float d_pitch = accel_pitch - gyro_pitch;
d_roll  = wrap_pi(d_roll);   // 处理角度过零跳变
d_pitch = wrap_pi(d_pitch);

ahrs->roll  = gyro_roll  + (1.0f - alpha) * d_roll;
ahrs->pitch = gyro_pitch + (1.0f - alpha) * d_pitch;
```

**为什么写成 `gyro + (1-alpha) * delta` 而不是 `alpha*gyro + (1-alpha)*accel`？**

其实两者**完全等价**：

$$
\theta = \alpha \cdot \theta_g + (1-\alpha) \cdot \theta_a
       = \theta_g + (1-\alpha) \cdot (\theta_a - \theta_g)
$$

写成第二种形式的好处是：
1. **物理意义更清晰**：陀螺积分作为基准，用加速度计的"误差"做小修正
2. **避免过零点跳变**：$\theta_a$ 可能在 +180° 和 -180° 之间跳变，先算差值再 wrap_pi 就没这个问题
3. **便于和 Mahony 对比**（Mahony 也是 `omega_corrected = omega + Kp*error`）

---

### 2.5 角度限幅 `wrap_pi`

```c
static float wrap_pi(float angle)
{
    while (angle >  PI_F) angle -= 2.0f * PI_F;
    while (angle < -PI_F) angle += 2.0f * PI_F;
    return angle;
}
```

**为什么需要？**
- 陀螺仪积分会让角度超过 ±180°
- atan2 返回值在 [-π, π]
- 如果 gyro_roll = 179° 而 accel_roll = -179°，差值是 -358°，这明显不对
- wrap_pi 后差值变成 +2°（正确理解：从 179° 到 -179° 其实是向前 2°）

---

## 第三部分：四元数版本 —— 和 Mahony 的异同

### 3.1 对照关系

| 步骤 | Mahony | 一阶互补（四元数版） |
|------|--------|---------------------|
| 归一化加速度 | ✅ | ✅ |
| 计算参考重力方向 | ✅ | ✅ |
| 计算参考磁场方向 | ✅ | ✅ |
| 叉积误差 | ✅ | ✅ |
| **积分项 (KI)** | ✅ 有 | ❌ **无** |
| 比例项 (KP) | ✅ 固定 KP | ✅ KP = (1-α)/dt · 0.5 |
| 四元数积分 | ✅ | ✅ |
| 四元数归一化 | ✅ | ✅ |

**核心区别就一行**：

```c
/* Mahony (有积分) */
integral_fb.x += ki * halfex * dt;   // 累积误差积分
gx += integral_fb.x;                  // 加积分修正
gx += kp * halfex;                    // 加比例修正

/* 一阶互补 (无积分) */
gx += kp * halfex;                    // 只有比例修正！
```

### 3.2 那积分项到底有什么用？

**答案：消除稳态偏差**

想像一个场景：陀螺仪有 0.1°/s 的零偏（bias）。

- **纯互补滤波**（无积分项）：误差会累积，角度慢慢漂。alpha 越大漂移越快。
- **Mahony**（有积分项 KI）：系统会"记住"过去的误差，把陀螺仪的零偏**估计**出来并补偿掉。这就是为什么 Mahony 的 yaw 漂移更小，姿态更稳。

**用控制理论术语**：
- 一阶互补 = **P 控制器**（比例控制）→ 有稳态误差
- Mahony = **PI 控制器**（比例+积分）→ 消除稳态误差

### 3.3 那为什么还要用一阶互补？

1. **简单**：代码少、参数少（只需要调一个 tau）
2. **稳定**：没有积分饱和（windup）问题
3. **够用**：对平衡车、简单无人机等应用完全 OK
4. **理解成本低**：先吃透互补，再学 Mahony 就只是"加个积分项"而已

---

## 第四部分：参数调参指南

### 4.1 时间常数 tau 的选择

| tau | alpha (100Hz 下) | 特性 | 适用场景 |
|-----|------------------|------|---------|
| 0.1s | 0.91 | 快速修正、抗振动差 | 运动剧烈、振动小 |
| 0.5s | 0.98 | 平衡（推荐默认） | 一般应用 |
| 1.0s | 0.99 | 非常平滑、修正慢 | 静止为主、振动大 |
| 5.0s | 0.998 | 几乎纯陀螺积分 | 高精度陀螺 + 长时静止 |

**理解公式**：

$$
\tau \approx \frac{\alpha}{(1-\alpha) \cdot f_s}
$$

- alpha 从 0.90 → 0.99，tau 从 0.09/fs → 0.99/fs，**变化 11 倍**
- 所以调参时，建议先**大范围扫** tau，再精细微调

### 4.2 调参实战步骤

1. **静止测试**：把 IMU 放桌上不动，看输出角度是否稳定
   - 若 roll/pitch 慢慢漂 → tau 太大，减小
   - 若 roll/pitch 高频抖动 → tau 太小，增大

2. **动态测试**：快速晃动然后停止
   - 看 gyro 积分是否跟得上（tau 太小会滞后）
   - 看停止后是否能快速回到正确值（tau 太大修正慢）

3. **振动测试**（如装到电机/四轴上）：
   - 振动环境下 alpha 要增大（更信陀螺）
   - 否则加速度计的振动噪声会污染姿态

---

## 第五部分：代码优化建议（从简单到高级）

### 5.1 优化 1：加速度计动态检测

当 `||a|| - g| > 阈值` 时，说明载体在做加速运动，此时加速度计不可靠：

```c
float a_mag = sqrtf(ax*ax + ay*ay + az*az);
float alpha_dyn = alpha;
if (fabsf(a_mag - 9.81f) > 2.0f) {
    alpha_dyn = 0.999f;  // 运动时几乎完全信陀螺
}
```

### 5.2 优化 2：陀螺仪零偏估计（就是 Mahony 的 KI 项）

```c
/* 在互补滤波基础上，加一个慢变量跟踪零偏 */
ahrs->gyro_bias_x += ki * halfex * dt;
gx_corrected = gx + ahrs->gyro_bias_x;
```

### 5.3 优化 3：yaw 辅助（磁力计/视觉/GPS）

没有磁力计时 yaw 会漂，有条件的话：
- 加磁力计（最简单，如 IST8310/RM3100）
- 用 GPS/视觉做航向辅助（如 ArduPilot 的 EKF3）

---

## 第六部分：和 Mahony 的最终对比

| 项目 | 一阶互补滤波 | Mahony 滤波 |
|------|-------------|-------------|
| 控制结构 | P（比例） | PI（比例+积分） |
| 参数 | tau（1个） | Kp, Ki（2个） |
| 零偏抑制 | 无（会漂） | ✅ 有 |
| 代码量 | 约 100 行 | 约 200 行 |
| 计算量 | 少（无叉积、无四元数可选） | 略多 |
| 典型姿态精度 | Roll/Pitch ~ 2-5°<br>Yaw: 漂移 ~0.5°/s | Roll/Pitch ~ 1-3°<br>Yaw: 有磁力计时 <2° |
| 适用场景 | 教学、平衡车、低成本应用 | 无人机、机器人、VR/AR |

---

## 一句话总结

> **一阶互补滤波 = 陀螺积分（快速）+ 加速度/磁力计修正（稳态），加权融合**
> **Mahony 滤波 = 一阶互补 + 陀螺仪零偏积分估计**

掌握了一阶互补，再看 Mahony 就只是多了几行积分代码而已 🎯
